# Go API base URL. The public API is used when this value is omitted.
BLOG_API_URL=http://localhost:8085/api/v1

# Optional browser override. Leave empty when Caddy or the Next.js rewrite exposes /api/v1.
# NEXT_PUBLIC_BLOG_API_URL=https://api.example.com/api/v1
